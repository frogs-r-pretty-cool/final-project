using UnityEngine;
/*
public class BallPickup : MonoBehaviour
{
    private void OnTriggerEnter2D(Collider2D other)
    {
        if (other.gameObject.CompareTag("Player"))
        {
            dudes thrower = other.GetComponent<dudes>();
            if (thrower != null && !thrower.HoldingBall())
            {
                thrower.GrabBall(transform.parent.gameObject);
            }
        }
    }
} */

